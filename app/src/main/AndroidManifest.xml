<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".KaetherApp"
        android:label="Aether"
        android:icon="@android:drawable/ic_menu_compass"
        android:theme="@style/Theme.Aether"
        android:extractNativeLibs="true"
        android:largeHeap="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Virtualization launch context for the target app -->
        <activity
            android:name=".proxy.ProxyActivity"
            android:exported="false"
            android:taskAffinity="com.aether.task_affinity"
            android:configChanges="orientation|keyboardHidden|screenSize" />

        <service
            android:name=".proxy.ProxyService"
            android:exported="false" />

        <provider
            android:name=".proxy.ProxyContentProvider"
            android:exported="false"
            android:authorities="com.aether.proxy" />

    </application>
</manifest>
